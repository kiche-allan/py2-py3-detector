# ── HIDDEN DIVISION BUG ──────────────────────────────────────────
# Paste into the detector and click Analyse.
# This is the dangerous kind: tests pass, production fails.
# Even denominators give the same result in both versions.
# Odd denominators reveal the difference.

# Test inputs — both versions agree, all tests pass
print(10 / 2)
print(100 / 4)
print(6 / 3)

# Production inputs — versions diverge here
print(1 / 3)
print(2 / 7)
print(5 / 8)
print(10 / 3)

# Real pattern: progress percentage
# py2: gives 0 (floors to int)   py3: gives 42.857...
completed = 3
total     = 7
print((completed / total) * 100)

# Real pattern: items per page
items     = 25
page_size = 10
print(items / page_size)